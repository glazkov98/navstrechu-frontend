export interface EventItemInterface {
    id: number
    title: string
    slug: string
    photoUrl: string
    type: string
    maxCountParticipants: number
    countParticipants: number
    price: number
    startDate: string
}
